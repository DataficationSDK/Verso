# Verso Notebook for VS Code

Open and execute `.verso` notebook files in VS Code / Cursor.

## Setup

Set `verso.hostPath` in settings to the path of your built `Verso.Host.dll`:

```json
{
  "verso.hostPath": "/path/to/tools/Verso/src/Verso.Host/bin/Debug/net8.0/Verso.Host.dll"
}
```
